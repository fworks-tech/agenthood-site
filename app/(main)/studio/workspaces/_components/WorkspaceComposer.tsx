// #TODO Workspaces: member picker + instruction textarea per docs/hackathon/spec.md:57-66,192-198
// - grid grouped by category (Engineering/Validation/Lifecycle/Knowledge) from _data/agents.ts
// - click toggles select (border-indigo-500), Mediator (the-mediator) NOT in grid (auto-included)
// - when >=1 selected, fade-in textarea + Start Workspace button -> collapses picker, shows 3-zone layout
'use client'

export default function WorkspaceComposer() {
  return null
}
