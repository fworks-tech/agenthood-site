// #TODO Workspaces: one member turn card per docs/hackathon/spec.md:197
// - plan -> act -> result, shows token stream + tool calls for that member
'use client'

export default function WorkspaceTurnCard() {
  return null
}
