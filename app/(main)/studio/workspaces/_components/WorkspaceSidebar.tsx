// #TODO Workspaces: left sidebar status cards per docs/hackathon/spec.md:42-44,196
// - lists selected members + Mediator first, status idle->thinking->working->waiting->done with CSS transitions
'use client'

export default function WorkspaceSidebar() {
  return null
}
